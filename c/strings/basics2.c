// we can also take input into a string in my ways ;
#include <stdio.h>
#include <string.h>
int main()
{
    char str[40]; // string ka size define krna pdega alag se input lene ke liye ;
    // scanf("%s", str); //  iss way main sentence ka sirf ek word hi input main aata h i.e pehla gap aate hi uske baad ke words nhi aa paate
    // printf("Your Input was : %s", str);
    // printf("\n");
    //  scanf("%[^\n]s", str); // iss way main hum scanf func.ka use krte huye pure sentence ko bhi as input le skte h
    //  printf("Your Input was : %s", str);
    //  printf("\n");
    // gets(str) ; // ye way most convinient h for taking a whole sentence as input
    // printf("Your Input was : %s", str);
}